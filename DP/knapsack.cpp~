#include<iostream>
#include<algorithm>

#define MAX_N=100
#define MAX_W 10000

using namespace std;

int main(){
	int n,w,a[MAX_N+1][MAX_W+1];
	cin<<n<<w<<endl;
	for(int i=0;i<n;i++){
		memset();
	}
return 0;
}
